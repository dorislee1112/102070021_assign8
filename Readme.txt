1. 按照pdf做出輸入ServerIP(MainActivity) 輸入問題(Calculator) 及顯示答案(Answer)的頁面
2. 在Calculator裡新增Clean功能，可以清空已經輸入的數字
3. 在Answer裡新增Home功能回到輸入IP頁面
4. 在Answer裡新增Use the answer功能，會自動把這一次的計算結果帶回Calculator頁面，可以連續做計算

遇到困難: 一開始做server的時候多做了一個applet，但怎麼顯示都顯示不出來，後來改成純粹用一個JFrame的window就好了